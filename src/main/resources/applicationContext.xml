<!-- applicationContext.xml -->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- para crear
    <context:property-placeholder location="classpath:datosEmpresa.propiedades"/>
    se debe agregar
     xmlns:context="http://www.springframework.org/schema/context"
     y ademas
     http://www.springframework.org/schema/context
     http://www.springframework.org/schema/context/spring-context.xsd">

    -->

    <context:property-placeholder location="classpath:datosEmpresa.propiedades"/>

    <!-- Creacion de la dependencia -->
    <bean id="miInforme" class="com.example.IoC.inyecciondependencia.Informe">

    </bean>

    <!-- Definición de un bean llamado "miBean" de tipo MiBean -->
    <bean id="miempleado" class="com.example.IoC.primer.DirectorEmpleado">
        <constructor-arg ref="miInforme"/>
        <property name="email" value="${email}" />
        <property name="nombreEmpresa" value="${nombreEmpresa}" />
    </bean>

    <!-- SecretarioEmpleado
      public void setCreacionInforme(CreacionInforme creacionInforme) {
        this.creacionInforme = creacionInforme;
    }
    <property name="creacionInforme"/>
    la creacion  de la propiedad name se debe a public void setCreacionInforme pero sin el prefijo set
    y con la primera letra en minuscula

    OJO los nombre de la propiedad  <property name="nombreEmpresa" deben ser iguales a los de la clase
     -->
    <bean id="miSecretario" class="com.example.IoC.primer.SecretarioEmpleado">
        <property name="creacionInforme" ref="miInforme"/>
        <property name="email" value="secretario@correo.com" />
        <property name="nombreEmpresa" value="LMS company" />
    </bean>

</beans>